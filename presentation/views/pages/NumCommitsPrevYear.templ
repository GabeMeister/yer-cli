package presentation_views_pages

import presentation_helpers "GabeMeister/yer-cli/presentation/helpers"

templ NumCommitsPrevYear(count int) {
	<div>
		<div>Number of commits made last year: { presentation_helpers.WithCommas(count) }</div>
	</div>
}
